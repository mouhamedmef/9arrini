import { GraduationCap, Search, User, Plus, BookOpen, Users, type LucideIcon } from "lucide-react"

export type Icon = LucideIcon

export const Icons = {
  logo: GraduationCap,
  search: Search,
  user: User,
  plus: Plus,
  discover: BookOpen,
  users: Users,
}

